# Copy to /etc/hanabi-canvas/realtime.env. This file contains deployment
# configuration, not credentials. Keep its origin value in sync with Nginx.
PORT=8080
ALLOWED_ORIGIN=https://hanabi.example.com
